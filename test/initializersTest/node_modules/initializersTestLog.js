var priest = require('priest')

function initializersTestLog() {
	
}
initializersTestLog.prototype = new priest.Expression()
initializersTestLog.prototype.execute = function() {
	var self = this
	this.runInput({
		_resultCallback: function(res) {
			console.log(res)
		}
	})
}

priest.enableModule(module).exportExpression({
	"name": "initializersTest.Log",
	"implementation": initializersTestLog
})